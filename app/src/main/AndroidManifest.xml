<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lishuai.modedemo">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ansell"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ansell"
        android:supportsRtl="true"
        android:name="MyApp"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="${_UMENG_KEY}" />
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        废弃-->
        <activity android:name=".MyListActivity"/>
        <activity android:name=".MyUnfinishedListActiity"/>
        <activity android:name=".MyScanActivity"
            android:windowSoftInputMode="stateHidden|stateUnchanged"
            />
    </application>

</manifest>